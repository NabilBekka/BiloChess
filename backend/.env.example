# ══════════════════════════════════════════
# BILO CHESS - Backend Environment Variables
# ══════════════════════════════════════════

# ── Base de données PostgreSQL (Neon) ──
DATABASE_URL=postgresql://username:password@ep-xxxxx.eu-west-1.aws.neon.tech/bilochess?sslmode=require

# ── Google OAuth ──
GOOGLE_CLIENT_ID=123456789-xxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxx

# ── JWT (JSON Web Token) ──
JWT_SECRET=change-this-to-a-very-long-random-string-at-least-32-characters
JWT_EXPIRES_IN=7d

# ── Server ──
PORT=5000

# ── SMTP (Gmail App Password) ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ton-email@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx

# ── Frontend URL (CORS) ──
FRONTEND_URL=http://localhost:3000
